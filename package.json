{
  "name": "karaoke-db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "initdb": "wrangler d1 execute karaoke-db --local --file=./schema.sql",
    "deploydb": "wrangler d1 execute karaoke-db --file=./schema.sql"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "wrangler": "^3.84.1"
  },
  "dependencies": {
    "britelite": "^1.1.0",
    "cookie": "^0.6.0",
    "itty-cors": "^0.3.6",
    "itty-router": "^4.0.26"
  }
}
